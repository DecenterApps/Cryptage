import React from 'react';
import { connect } from 'react-redux';

import './Locations.scss';

const Locations = () => (
  <div className="locations-wrapper">locations</div>
);

Locations.propTypes = {};

export default connect(null, null)(Locations);
