import React from 'react';
import { connect } from 'react-redux';

import './BoostersMenu.scss';

const BoostersMenu = () => (
  <div className="boosters-menu-wrapper">Boosters Menu</div>
);

BoostersMenu.propTypes = {};

export default connect(null, null)(BoostersMenu);
